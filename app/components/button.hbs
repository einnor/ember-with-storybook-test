<PaperButton @raised={{raised}} @fab={{fab}} @primary={{if isPrimary true}} @warn={{if isWarn true}} @accent={{if isAccent true}}>
  {{#if icon}}
    {{paper-icon "accessibility"}}
  {{/if}}
  {{label}}
</PaperButton>
