<PaperButton @raised={{raised}} @fab={{fab}} @primary={{if isPrimary true}} @warn={{if isWarn true}} @accent={{if isAccent true}} @disabled={{if disabled true}}>
  {{#if icon}}
    {{paper-icon "accessibility"}}
  {{/if}}
  {{label}}
</PaperButton>
